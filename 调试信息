1.自定义读取数据集

2.图像处理

3.网络设计
注意力网络   网络层级 上5下5，比较深

4.模型训练
模型训练经验：小样本量时，未加入数据增强，出现了过拟合现象；
提升方式：a，Adam的优化器加入ReduceLROnPlateau，在验证集时参考优化器数据，进行交叉验证。加入测试后效果降低，原因是过早降低学习率收敛过快，导致陷入局部最优解。
b，网络层级：在全连接层加入，dropou防止过拟合。在unet的标准双层调用网络中，自带了BatchNorm2d可防止网络震荡稳定网络层级。
	每次输出都加入dropout，防止过拟合。测试常用数值为0.1-0.3，如果不需要启动dropout设置为0。当学习特征不足时不启用dropout。
c. 原本的batch设置为4，需要测试batch为1会不会更适合小样本。
   	 每次训练的batch对模型性能的影响：小批次往往能提供更好的泛化能力，但收敛慢，计算效率低；而大批次训练则可能加速收敛，但有过拟合的风险。
	反卷积(上采样操作) :降低通道数，从而在解码器中恢复空间分辨率，为后续特征融合和卷积做好准备。
d. 启用样本增强，是否上下，左右翻转；有些样本不适合变换。gamma变换指数0.3-0.6。
  先下采样，图像变窄通道变深in-64-128-256-512-1024，再上采样，图像变宽通道变少1024-512-256-128-64-out

5.模型测试

6.确定标准，进行模型评估





